StopMusic(STRING which, INT unk0, INT unk1, STRING unk2)
Stops specified music. @BGM* wildcard can be used to stop any background music.

LoadMovie(STRING handle, INT DrawPriority, INT X, INT Y, STRING unk3, STRING unk4, STRING file, STRING unk5)
Loads movie from specified file and binds it to handle. Full path expected.

LoadTexture(STRING handle, INT unk0, INT unk1, INT unk2, STRING file)
Load texture from specified file and binds it to handle. Full path expected.

Set(VAR var)
Sets var to value specified by last call to SetParam.

Get(VAR var)
Retrieves value of var and sets it as parameter for next function call.

SetParam(ANY value)
Sets value as parameter for next function call.

CallScript(STRING file)
Calls specified nss script file. Full path expected.
NOTE: Files in nss/ have .nsb extension, perhaps these are compiled files on virtual fs?

Concat()
Concats two previously specified parameters into one.

Callback(STRING key, STRING file)
Executes script specified by file when key is pressed.

SetDisplayState(STRING buffer, STRING state)

StartAnimation(STRING handle, INT time_required, INT x, INT y, STRING tempo, STRING wait)
